# =============================================================================
# notion_bridge - Dependencias Python
# =============================================================================
# Python target: 3.11+
#
# IMPORTANTE: este bridge usa `client.databases.query(database_id, ...)` para
# leer paginas. La version 3.x del SDK (rama `ramnes/notion-sdk-py`) elimino
# ese metodo y lo reemplazo por `client.data_sources.query(data_source_id, ...)`,
# lo cual rompe la API documentada en /docs/notion_schema.md.
#
# Pineamos a la serie 2.x (la que expone la API usada por el spec).
# Si necesitas migrar a 3.x, refactoriza `client.NotionClient.query_database`
# para que primero obtenga el data_source_id y luego llame a data_sources.query.
# =============================================================================

notion-client>=2.2.0,<3.0.0
